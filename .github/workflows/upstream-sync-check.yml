name: Upstream sync check

on:
  schedule:
    - cron: '0 6 * * *'   # 06:00 UTC nightly (~07:00/08:00 NL). Times are UTC; adjust as needed.
  workflow_dispatch: {}    # allow manual runs from the Actions tab

permissions:
  contents: read
  issues: write

concurrency:
  group: upstream-sync-check
  cancel-in-progress: true

env:
  # This repo is NOT a GitHub fork, so .parent is empty in the API,
  # and the REST "compare" endpoint's cross-repo owner:branch shorthand
  # only works within a real fork network anyway (hence the 404 we used
  # to get). So this step uses plain git instead of `gh api .../compare`.
  # For the same reason there is no usable /compare/ web URL between the
  # two repos -- we link upstream's commit list and inline the missing
  # commits instead.
  UPSTREAM_REPO: christopherklint97/streamvault
  # How many missing upstream commits to list in the issue body.
  MAX_LISTED: 20

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Compare fork against upstream
        id: compare
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          FORK="${{ github.repository }}"
          UPSTREAM="${UPSTREAM_REPO}"

          if [ -z "$UPSTREAM" ]; then
            echo "::error::UPSTREAM_REPO is not set."
            exit 1
          fi

          git clone --quiet "https://x-access-token:${GH_TOKEN}@github.com/$FORK.git" repo
          cd repo

          FORK_BRANCH="$(git remote show origin | sed -n '/HEAD branch/s/.*: //p')"

          git remote add upstream "https://github.com/$UPSTREAM.git"
          git fetch --quiet upstream
          UPSTREAM_BRANCH="$(git remote show upstream | sed -n '/HEAD branch/s/.*: //p')"

          echo "Fork:     $FORK ($FORK_BRANCH)"
          echo "Upstream: $UPSTREAM ($UPSTREAM_BRANCH)"

          # left  = commits only on the fork (local-only / "ahead")
          # right = commits only on upstream (fork is missing them / "behind")
          read -r AHEAD BEHIND <<< "$(git rev-list --left-right --count \
            "origin/$FORK_BRANCH...upstream/$UPSTREAM_BRANCH")"

          if [ "$AHEAD" -eq 0 ] && [ "$BEHIND" -eq 0 ]; then
            STATUS="identical"
          elif [ "$AHEAD" -eq 0 ]; then
            STATUS="behind"
          elif [ "$BEHIND" -eq 0 ]; then
            STATUS="ahead"
          else
            STATUS="diverged"
          fi

          echo "status=$STATUS behind=$BEHIND local-only=$AHEAD"

          {
            echo "upstream=$UPSTREAM"
            echo "upstream_branch=$UPSTREAM_BRANCH"
            echo "fork_branch=$FORK_BRANCH"
            echo "status=$STATUS"
            echo "behind=$BEHIND"
            echo "ahead=$AHEAD"
          } >> "$GITHUB_OUTPUT"

          # Inline the commits we are missing. There is no working cross-repo
          # /compare/ URL for non-fork repos, so the list IS the diff view.
          {
            echo "missing<<__MISSING_EOF__"
            if [ "$BEHIND" -gt 0 ]; then
              git log --max-count="$MAX_LISTED" \
                --format="- [\`%h\`](https://github.com/$UPSTREAM/commit/%H) %s" \
                "origin/$FORK_BRANCH..upstream/$UPSTREAM_BRANCH"
              if [ "$BEHIND" -gt "$MAX_LISTED" ]; then
                echo "- _…and $((BEHIND - MAX_LISTED)) more._"
              fi
            fi
            echo "__MISSING_EOF__"
          } >> "$GITHUB_OUTPUT"

          {
            echo "### Upstream sync check"
            echo ""
            echo "| | |"
            echo "|---|---|"
            echo "| Fork | \`$FORK\` (\`$FORK_BRANCH\`) |"
            echo "| Upstream | \`$UPSTREAM\` (\`$UPSTREAM_BRANCH\`) |"
            echo "| Status | \`$STATUS\` |"
            echo "| Behind upstream | **$BEHIND** |"
            echo "| Local-only commits | $AHEAD |"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Open / update / close tracking issue
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # `gh` resolves the target repo from the git remote, and this job
          # never checks out the repo -- without GH_REPO every `gh issue`
          # call dies with "fatal: not a git repository".
          GH_REPO: ${{ github.repository }}
          OWNER: ${{ github.repository_owner }}
          UPSTREAM: ${{ steps.compare.outputs.upstream }}
          UPSTREAM_BRANCH: ${{ steps.compare.outputs.upstream_branch }}
          FORK_BRANCH: ${{ steps.compare.outputs.fork_branch }}
          STATUS: ${{ steps.compare.outputs.status }}
          BEHIND: ${{ steps.compare.outputs.behind }}
          AHEAD: ${{ steps.compare.outputs.ahead }}
          MISSING: ${{ steps.compare.outputs.missing }}
        run: |
          set -euo pipefail

          LABEL="upstream-sync"
          TITLE="Fork is behind upstream ($UPSTREAM)"

          # Requires Issues to be enabled on the repo. If someone turns them
          # off again, say so plainly instead of failing on a confusing
          # "not a git repository" further down.
          if [ "$(gh api "repos/$GH_REPO" --jq .has_issues)" != "true" ]; then
            echo "::error::Issues are disabled on $GH_REPO, so the tracking issue cannot be managed. Enable them under Settings -> Features -> Issues."
            exit 1
          fi

          gh label create "$LABEL" --color FBCA04 \
            --description "Fork out of sync with upstream" 2>/dev/null || true

          NUM="$(gh issue list --state open --label "$LABEL" \
                  --json number --jq '.[0].number // empty')"

          if [ "$BEHIND" -eq 0 ]; then
            echo "In sync with $UPSTREAM."
            if [ -n "$NUM" ]; then
              gh issue comment "$NUM" --body "Back in sync with $UPSTREAM. Closing automatically."
              gh issue close "$NUM"
            fi
            exit 0
          fi

          WARN=""
          if [ "$STATUS" = "diverged" ]; then
            WARN="> Note: branches have diverged ($AHEAD local commit(s)) — expect a merge, not a fast-forward."
          fi

          cat > body.md <<EOF
          \`${GITHUB_REPOSITORY}\` (\`$FORK_BRANCH\`) is **$BEHIND commit(s) behind** upstream \`$UPSTREAM\` (\`$UPSTREAM_BRANCH\`).

          - Compare status: \`$STATUS\`
          - Behind upstream by: **$BEHIND**
          - Local-only commits: **$AHEAD**

          ## Upstream commits not in this repo

          $MISSING

          Full upstream history: <https://github.com/$UPSTREAM/commits/$UPSTREAM_BRANCH>

          ## Sync your fork

          ~~~bash
          # one-time: add the upstream remote
          git remote add upstream https://github.com/$UPSTREAM.git

          git fetch upstream
          git checkout $FORK_BRANCH
          git merge upstream/$UPSTREAM_BRANCH      # or: git rebase upstream/$UPSTREAM_BRANCH
          git push origin $FORK_BRANCH
          ~~~

          $WARN

          _Checked nightly by \`.github/workflows/upstream-sync-check.yml\`. This issue updates itself and closes when back in sync._
          EOF

          if [ -n "$NUM" ]; then
            gh issue edit "$NUM" --title "$TITLE" --body-file body.md
            echo "Updated issue #$NUM."
          else
            gh issue create --title "$TITLE" --label "$LABEL" --body-file body.md
            echo "Created tracking issue."
          fi
